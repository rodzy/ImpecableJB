@model Nullable<bool>

@{
    bool variable = false;
    if (Model != null)
    {
        variable = (bool)Model;
    }
}

<div class="form-check">
    @Html.RadioButton("", "true", variable, new { @class = "form-check-input" })
    @Html.Label("Procesado", new { @class = "form-check-label" })
</div>
<div class="form-check">
    @Html.RadioButton("", "false", !variable, new { @class = "form-check-input" })
    @Html.Label("En proceso", new { @class = "form-check-label" })
</div>
